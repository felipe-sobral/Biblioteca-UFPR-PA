<?php

  /*
    E-MAIL: preg_replace('/[^a-zA-Z0-9@._\-_]/', '',$variável);
    USUÁRIO: preg_replace('/[^a-z_]/', '',$variável);
    NOME: preg_replace('/[^a-zA-Z\ _]/', '',$variável);
    NIVEL: preg_replace('/[^0-9_]/', '',$variável);
    DATA: preg_replace('/[^0-9\-_]/', '',$variável);
  */

?>
